# TTT — Work Time Tracker

Electron + React + Tailwind + SQLite. macOS only. See [plan.md](plan.md) for roadmap and status.

```bash
npm install
npm run dev      # develop
npm test         # unit tests
npm run build    # production build
```
